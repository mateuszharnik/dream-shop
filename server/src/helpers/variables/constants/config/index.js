const DEFAULT_PORT = '3000';
const DEFAULT_CLIENT_URL = 'http://localhost:4200';
const DEFAULT_DB_URL = 'localhost/dream-shop';

const PRODUCTION = 'production';
const DEV = 'development';

module.exports = {
  DEFAULT_PORT,
  DEFAULT_CLIENT_URL,
  DEFAULT_DB_URL,
  PRODUCTION,
  DEV,
};
