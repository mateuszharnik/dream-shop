const DEFAULT_SERVER_PORT = '3000';
const DEFAULT_CLIENT_PORT = '4200';
const DEFAULT_SERVER_URL = `http://localhost:${DEFAULT_SERVER_PORT}`;
const DEFAULT_CLIENT_URL = `http://localhost:${DEFAULT_CLIENT_PORT}`;
const DEFAULT_DB_URL = 'localhost/dream-shop';

const PRODUCTION = 'production';
const DEV = 'development';

module.exports = {
  DEFAULT_SERVER_PORT,
  DEFAULT_CLIENT_PORT,
  DEFAULT_CLIENT_URL,
  DEFAULT_SERVER_URL,
  DEFAULT_DB_URL,
  PRODUCTION,
  DEV,
};
