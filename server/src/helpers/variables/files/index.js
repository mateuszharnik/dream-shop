const pathToFileNotCorrectMessage = 'Ścieżka do pliku jest nieprawidłowa.';
const pathToFileRequiredMessage = 'Ścieżka do pliku nie może być pusta.';

const fileTypeNotCorrectMessage = 'Typ pliku jest nieprawidłowy.';
const fileTypeRequiredMessage = 'Musisz podać typ pliku.';

const fileSizeMaxMessage = 'Plik nie może ważyć więcej niż 5 MB.';

const avatarNewSize = 150;

const productImageNewHeight = 1200;
const productImageNewWidth = 900;

module.exports = {
  pathToFileNotCorrectMessage,
  pathToFileRequiredMessage,
  fileTypeNotCorrectMessage,
  fileTypeRequiredMessage,
  fileSizeMaxMessage,
  avatarNewSize,
  productImageNewHeight,
  productImageNewWidth,
};
